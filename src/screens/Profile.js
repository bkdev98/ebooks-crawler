import React from 'react';
import Typography from 'material-ui/Typography';

const Profile = () => (
  <div>
    <Typography variant="title">Profile Page</Typography>
  </div>
);

export default Profile;
