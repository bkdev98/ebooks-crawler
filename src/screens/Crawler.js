import React from 'react';
import Typography from 'material-ui/Typography';

const Crawler = () => (
  <div>
    <Typography variant="title">Crawler Page</Typography>
  </div>
);

export default Crawler;
